spring:
  application:
    name: organization-service # Logical name of the service that will be registered with Eureka
  profiles:
    active: dev
  cloud:
    config:
      uri: http://configserver:8071